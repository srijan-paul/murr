{
  "name": "murr",
  "version": "0.1.1",
  "description": "A tool to detect vulnerable packages in your project",
  "main": "index.js",
  "types": "index.d.ts",
  "repository": "https://github.com/srijan-paul/murr",
  "author": "Srijan Paul",
  "license": "LoS",
  "private": false,
  "devDependencies": {
    "purescript": "^0.14.5",
    "spago": "^0.20.7"
  },
  "dependencies": {},
  "scripts": {
    "build": "spago install && spago build && tsc --build",
    "prepublish": "spago install && spago build"
  }
}
